# Synthetic fixture vault

Every person, company, and event below is **invented for testing**. None of it comes
from anyone's real notes, and nothing here should ever be replaced with real vault
content: this directory is committed to a public repository.

It exists so the sync can be tested end to end without a real vault present, which is
also what lets CI run these tests at all.
